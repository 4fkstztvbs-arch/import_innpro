name: Penta sync

on:
  schedule:
    # StoItemShoptet_El's GetResult is only available 21:00-08:00 CET/CEST (per Penta's own
    # ResultTypeInfo table). 22:20 UTC = 00:20/01:20 CET/CEST depending on DST — inside that
    # window, and inside Shoptet's own 00:00-07:00 "úplný import" window.
    - cron: '20 22 * * *'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install --omit=dev

      - name: Run Penta transform
        env:
          PENTA_URL: ${{ secrets.PENTA_URL }}
          PENTA_USERNAME: ${{ secrets.PENTA_USERNAME }}
          PENTA_PASSWORD: ${{ secrets.PENTA_PASSWORD }}
          PENTA_OUT: output/penta.xml
          HEUREKA_PRICE_OVERRIDE: '1'
          # Temporary: first test run only imports categories with a confirmed match onto our
          # existing tree (reports/penta-kategorie-2026-08-24.md). Remove once the rest of the
          # categories are paired by hand and added to scripts/penta-mapping.json.
          PENTA_ONLY_MAPPED_CATEGORIES: '1'
          # Temporary: keep this first test import hidden from customers until it's reviewed.
          # Set to 'visible' (or remove) once ready to publish.
          PENTA_VISIBILITY: 'hidden'
        run: node scripts/transform-penta.js

      - name: Pull latest before cross-supplier dedupe
        run: |
          git stash -u
          git pull --rebase origin main
          git stash pop

      - name: Cross-supplier dedupe
        run: node scripts/dedupe-cross-supplier.js

      - name: Commit updated output
        run: |
          git config user.name "penta-sync-bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --staged --quiet && echo "No changes" || git commit -m "Automatický Penta sync $(date -u +%Y-%m-%dT%H:%MZ)"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main
            if git push; then
              echo "Push succeeded on attempt $i"
              break
            fi
            echo "Push failed (attempt $i) — another workflow likely pushed at the same time. Retrying in $((i*8))s..."
            sleep $((i*8))
            if [ "$i" = "5" ]; then
              echo "::error::Push failed after 5 attempts"
              exit 1
            fi
          done
